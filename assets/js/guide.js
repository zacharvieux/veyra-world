const search=document.querySelector('#glossary-search');if(search){const entries=[...document.querySelectorAll('.glossary-entry')];const count=document.querySelector('#glossary-count');const update=()=>{const q=search.value.trim().toLowerCase();let shown=0;entries.forEach(el=>{const hay=(el.dataset.term+' '+el.textContent).toLowerCase();const visible=!q||hay.includes(q);el.hidden=!visible;if(visible)shown++;});if(count)count.textContent=`${shown} ${shown===1?'entry':'entries'}`;};search.addEventListener('input',update);update();}
